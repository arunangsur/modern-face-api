# requirements.txt (Optimized for low-memory runtime)

fastapi
uvicorn[standard]
python-multipart

# Pinning to a stable, CPU-only version of TensorFlow
tensorflow-cpu==2.15.0

# DeepFace and its dependencies
deepface
tf-keras
pandas
Pillow
numpy
